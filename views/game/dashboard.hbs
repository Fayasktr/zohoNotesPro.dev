<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-dark: #0a0a0c;
            --card-bg: #111114;
            --accent: #6d5dfc;
            --text-main: #e1e1e6;
            --text-dim: #8e8e99;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-main);
            overflow-x: hidden;
        }

        .topic-card {
            background: var(--card-bg);
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }

        .topic-card:hover {
            transform: translateY(-5px);
            border-color: var(--accent);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4), 0 0 20px rgba(109, 93, 252, 0.1);
        }

        .topic-card::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, transparent, rgba(109, 93, 252, 0.05));
            opacity: 0;
            transition: opacity 0.3s;
        }

        .topic-card:hover::after {
            opacity: 1;
        }

        .custom-scrollbar::-webkit-scrollbar {
            width: 8px;
        }

        .custom-scrollbar::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.02);
        }

        .custom-scrollbar::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
        }

        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
            background: var(--accent);
        }
    </style>
</head>

<body class="min-h-screen py-12 px-8 custom-scrollbar">
    <div class="max-w-7xl mx-auto">
        <!-- Header -->
        <header class="flex flex-col md:flex-row justify-between items-start md:items-center mb-16 gap-6">
            <div class="space-y-2">
                <div class="flex items-center gap-2 text-[var(--accent)] font-black text-xs uppercase tracking-widest">
                    <span class="w-8 h-[2px] bg-[var(--accent)]"></span>
                    JS MASTER JOURNEY
                </div>
                <h1 class="text-5xl font-black text-white leading-none">THE NEW WORLD</h1>
                <p class="text-[var(--text-dim)] font-medium">Master the execution, one layer at a time.</p>
            </div>

            <div class="flex items-center gap-4">
                <div class="glass px-6 py-3 rounded-2xl border border-white/5 bg-white/5">
                    <div class="text-[10px] font-black text-[var(--text-dim)] uppercase">Total Points</div>
                    <div class="text-xl font-black text-yellow-500">{{user.points}}</div>
                </div>
                <a href="/"
                    class="px-6 py-4 bg-white/5 hover:bg-white/10 rounded-2xl border border-white/10 transition-all font-bold text-sm">
                    EXIT TO NOTES
                </a>
            </div>
        </header>

        <!-- Topics Grid -->
        <section>
            <div class="flex items-center justify-between mb-8">
                <h2 class="text-2xl font-black flex items-center gap-3">
                    <i data-lucide="compass" class="text-[var(--accent)] w-6 h-6"></i>
                    Learning Path
                </h2>
                <div
                    class="text-sm font-bold text-[var(--text-dim)] bg-white/5 px-4 py-2 rounded-full border border-white/5">
                    {{availableTopics.length}} TOPICS AVAILABLE
                </div>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                {{#each availableTopics}}
                <div class="topic-card p-6 rounded-[2rem] cursor-pointer group"
                    onclick="window.location.href='/game/map/{{this}}'">
                    <div
                        class="w-14 h-14 bg-white/5 rounded-2xl flex items-center justify-center mb-6 border border-white/5 group-hover:bg-[var(--accent)] transition-all">
                        <i data-lucide="box" class="w-6 h-6 text-white"></i>
                    </div>
                    <h3 class="text-lg font-black capitalize mb-2 text-white">{{this}}</h3>
                    <p class="text-sm text-[var(--text-dim)] leading-relaxed">Systematic deep-dive into {{this}}
                        concepts.</p>

                    <div class="mt-6 flex items-center justify-between">
                        <span
                            class="text-[10px] font-black text-[var(--accent)] uppercase group-hover:translate-x-1 transition-transform inline-flex items-center gap-1">
                            START MISSION <i data-lucide="arrow-right" class="w-3 h-3"></i>
                        </span>
                        <i data-lucide="lock" class="w-4 h-4 opacity-20"></i>
                    </div>
                </div>
                {{/each}}
            </div>
        </section>
    </div>

    <script>
        lucide.createIcons();
    </script>
</body>

</html>